{
  "name": "telegram-bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "migrate:up": "npx sequelize-cli db:migrate",
    "migrate:fresh": "npx sequelize-cli db:drop && npm run migrate:up",
    "migrate:fresh:seed": "npm run migrate:fresh && npx sequelize-cli db:seed:all"
  },
  "type": "module",
  "engines": {
    "node": ">=18",
    "npm": ">=8"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "mariadb": "^3.2.0",
    "sequelize": "^6.32.1",
    "telegraf": "^4.12.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "sequelize-cli": "^6.6.1"
  }
}
